# FaceLocking MQTT broker — append to Mosquitto or use with: mosquitto -c mosquitto.facelocking.conf
# Allows ESP8266 on your Wi-Fi LAN to connect (not just localhost).

listener 1883 0.0.0.0
allow_anonymous true
